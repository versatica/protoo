{
  "private": true,
  "name": "protoo-test",
  "scripts": {
    "test": "tap --bail --color --reporter=spec test.js"
  },
  "dependencies": {
    "tap": "^10.7.3"
  },
  "devDependencies": {
    "gulp": "git://github.com/gulpjs/gulp.git#4.0",
    "gulp-eslint": "^4.0.1"
  }
}
