{
  "name": "protoo-test",
  "private": true,
  "scripts": {
    "lint": "eslint -c .eslintrc.js __tests__",
    "test": "jest",
    "coverage": "cd .. && jest --coverage --config test/package.json && open-cli coverage/lcov-report/index.html"
  },
  "jest": {
    "verbose": true,
    "testEnvironment": "node",
    "rootDir": "../"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-plugin-jest": "^22.4.1",
    "jest": "^24.7.1",
    "jest-tobetype": "^1.2.3",
    "open-cli": "^9.0.0"
  }
}
