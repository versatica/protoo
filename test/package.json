{
  "private": true,
  "name": "protoo-test",
  "scripts": {
    "test": "tap --bail --color --reporter=spec test.js"
  },
  "dependencies": {
    "tap": "^11.0.1"
  },
  "devDependencies": {
    "gulp": "^4.0.0",
    "gulp-eslint": "^4.0.1"
  }
}
